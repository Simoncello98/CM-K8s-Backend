apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cm
  annotations:
    ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - host: localhost
  - http:
      paths:
      - path: /c/
        pathType: Prefix
        backend:
          service:
            name: cm-campusservice
            port:
              number: 80
      - path: /
        pathType: Prefix
        backend:
          service:
            name: cm-campusservice
            port:
              number: 80