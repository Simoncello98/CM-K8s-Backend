apiVersion: apps/v1
kind: Deployment
metadata:
  name: cm-campusservice
  labels:
    app: cm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cm
  template:
    metadata:
      labels:
        app: cm
    spec:
      containers:
      - name: cm-campusservice
        image: scionticdx/campusservice